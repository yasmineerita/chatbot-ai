import { Box, AppBar, Toolbar } from "@mui/material";

export default function Header() {
  return (
    <Box>
      <AppBar>
          <Toolbar>

          </Toolbar>
      </AppBar>
    </Box>
  )
}